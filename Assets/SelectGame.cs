using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SelectGame : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Debug.Log("hej fra select game");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
